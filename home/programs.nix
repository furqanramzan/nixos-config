{
  programs = {
    home-manager = {
      enable = true;
    };
    
    # Browsers
    firefox = {
      enable = true; # TODO: Add extensions here
    };
    chromium = {
      enable = true;
    };
  };
}
